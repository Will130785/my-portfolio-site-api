const sum = (num1, num2) => {
  return num1 + num2
}

const greeting = (input) => {
  return input
}

const data = {
  one: 1
}

const isNull = () => {
  return null
}

const checkValue = (val) => {
  return val
}

const createUser = () => {
  const user = {
    firstName: 'Will',
    lastName: 'Constable'
  }

  return user
}

data['two'] = 2

module.exports = {
  sum,
  greeting,
  data,
  isNull,
  checkValue,
  createUser
}
